import { ImageBackground, StyleSheet } from "react-native";
import { TEMA } from "../../tema/tema";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: TEMA.COLORS.BACKGROUND_TEMA,    
  }
})